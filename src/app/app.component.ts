import { Component } from '@angular/core';

@Component({
    selector: 'app-root',
    template: `
        <div class="appWrapper">
            <app-shell></app-shell>
        </div>
    `
})
export class AppComponent { }
